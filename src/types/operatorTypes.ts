export enum LogicalOperator {
  EQ = "eq",
  NEQ = "neq",
  GT = "gt",
  LT = "lt",
  GTE = "gte",
  LTE = "lte",
  ANY = "any",
  ALL = "all",
}

export enum JoinOperator {
  AND = "and",
  OR = "or",
}